---
name: journal-club-ppt
description: 将上传的科研论文直接制作成图文平衡、实验逻辑完整的中文组会 PPTX 和 PDF；也用于继续或修改已有文献汇报。默认一次完成，缓存论文与图源，采用大字号解释和原始 Figure，减少重复解析及排版返工。
---

# 文献组会 PPT：一次完成

用户只需给论文或现有项目及修改要求。自动完成阅读、逐页设计、原图裁切、制作、导出和检查，不要求用户选择模式，不在目录或样张阶段停下等待确认。真正缺失且影响科学结论的材料需要说明；可独立推进的工作继续完成。

本技能的标准流程不依赖任何其他 skill，也不读取过去的聊天。新设备先按下节检查环境，正文阅读与视觉判断仍由具备图文能力的模型完成；安装脚本不会自行理解论文或生成科学结论。

## 新设备与一次启动

1. 先定位本技能根目录。用平台提供的 Python（有 `load_workspace_dependencies` 时优先用它）；没有该工具时使用系统 Python 3.10+。运行 `python SKILL/scripts/run.py doctor`。
2. 缺 Python 包时运行 `python SKILL/setup.py`，在本技能 `.venv` 中安装固定版本；后续统一通过 `scripts/run.py` 运行。不要借用 `presentation-skill/.venv` 或其他技能路径。缺字体/导出器时只读 [README.md](README.md) 的对应处理，不反复全盘搜索。
3. 首次安装用 `python SKILL/scripts/run.py demo --workspace EMPTY_DEMO_DIR` 内部检验代表页面并查看渲染；这是排版样例，绝不当成真实研究数据。例图或缓存可用且引擎、依赖及字体未变时不反复执行。
4. 确认模型能查看 PDF 页面与图片。图像生成属于宿主可选能力，不能由 Python 包检查推断可用；不可用时使用内置原生流程图或 `evidence-grid`，同时保持统一排版。只在需要概念视觉时读 [visual-standard.md](references/visual-standard.md)。
5. 上述可执行检查完成后直接处理论文。图文能力、导出软件、字体许可或图像生成权限无法由复制一个 skill 获得；遇到缺失时说明具体未完成项，不承诺无条件同等效果。

## 先定位，再执行

- 新论文：读 [paper-to-deck.md](references/paper-to-deck.md)，完成科学内容与逐页方案，随即制作。默认中文正式组会；时长和页数按实验数量判断，机制长文可参考 26–32 页，但不把 32 页作为所有论文的硬指标。
- 已有项目/“继续”：先读项目 `state.json`、`deck.json` 或原有源文件和最近 QA，只加载未完成或受影响的页。保留既有构建入口，不为采用本 skill 迁移或重写成熟项目。
- 用户要求修改：从请求推断修改范围，锁住未授权变化的页数、页序、实验分组、科学结论和指定保留页。读 [revision.md](references/revision.md)。不把局部修改扩成全文重构。

这些是内部处理规则，不向用户呈现三个入口。

## 直接采用的定稿标准

- 按一个连续实验问题组织一页；同一 Figure 的连续 panel 合并，复杂实验允许连续 2–3 页。检查所有 panel，不能为了凑页数漏掉关键对照或干预。
- 结果页：**结论型主标题 → 一行条件/模型 → 分隔线 → 原图＋2–3 个解释模块 → 浅底“核心结论＋补充说明”**。
- 解释模块写清目的/设计、观察结果和含义。底部补充解释或证据边界，避免复述核心结论。实验为何衔接写入正文必要处或备注，不堆“下一实验”“原文页码”等微型提示。
- 中部图文约各占一半。按图的宽高选择左右或上下排布；原图的主要图像、组别、坐标、显著性必须可辨。密集页优先重排、精简重复文字，不自动缩小正文或裁掉关键图例。
- 默认 16:9、微软雅黑、深蓝 `18354A`、酒红 `8B404A`、浅底 `F1F5F3`；标题 32 pt、条件 18 pt、模块标题 20 pt、正文 19 pt、核心 20 pt、补充 18.5 pt。全套同一体系。用户提供其他模板时服从用户。
- 新建汇报的首页默认直接放一张原文 PDF 首页截图作为主体，优先清楚呈现期刊、原文题目和作者，不默认生成 AI 封面；具体截图与布局见 [paper-to-deck.md](references/paper-to-deck.md)。用户另有封面要求时服从用户。
- 封面有现成版本且用户要求保留时原样保留；原本可编辑的封面保持可编辑，只有图片输入才按图片保留。不擅自加姓名、日期、单位或徽标。
- 背景、机制、逻辑和总结页也要有实质解释，不能只靠插画。AI 用于概念图，默认无文字底图＋可编辑文字；已接受的插画继续复用。用户要求整页 AI 图时允许采用，但实验数据仍单独嵌入原图。

## 科学底线

动物照片、组织染色、IF、WB、qPCR、流式、BLI、对接、统计图和网络图均从原论文提取，保留比例、组别、轴、通道、标尺和显著性，不交给图像模型重绘。不凭柱高编数值，不把预测当验证、表达当功能、相关当因果。方法与图注冲突、代表值与均值、残余效应和缺失补充材料要准确定位，不能编造解决。

## 节省 token 与返工

1. 先缓存 PDF 文本、页索引、图源、panel 映射和来源哈希；后续只读相关页面，不重复整篇输出。科学阅读不能用缓存命中替代。
2. 科学内容、主题、布局分别保存；优先修改数据文件，使用同一个构建脚本。已有源文件时继续用原脚本。
3. 正式批量制作前，内部检查三种代表页：背景页、普通结果页、最密集结果页。只修实际问题，不把“样张确认”变成用户关卡。
4. 原图和已接受 AI 图按哈希复用；不因改文字重跑图像生成。默认只加载本技能和与任务相关的参考文件。论文逻辑、术语统一、原图检查与演示文稿构建规则已内置；其他 skill 仅在用户明确调用或超出本技能对象能力时按需使用。
5. 修改后机器检查全套页数、图源和锁定项；视觉检查改变的页及其相邻页。全局主题变化需检查全套。最终须完整导出一次，确保交付文件与检查文件一致。
6. 每个完成阶段记录短 `state.json`：输入哈希、当前源文件、已完成项、待办、下一步。中断后从它恢复；技能本身不能在额度恢复时自行唤醒，不承诺未经配置的自动续跑。

## 可直接执行的工具

读 [build-and-check.md](references/build-and-check.md) 获取数据格式与命令。脚本可直接运行，无需每次把源码读入上下文：

- `scripts/project.py`：缓存、按页取文本/预览、批量裁图、短检查点。
- `scripts/render_deck.py`：新项目的原生 PPTX 图文模板、变更页识别、局部预览、PDF 导出与渲染。
- `scripts/audit.py`：原图哈希/页序/结论锁定、文本尺寸和图源完整性检查。
- `scripts/verify_source_pixels.py`：原始 PDF 裁图与实际使用图逐像素核验。
- `scripts/run.py`：自动选择本技能运行环境，提供 doctor、project、build、finish、audit、demo 入口。

PDF 优先由 LibreOffice 导出；Windows 可使用已安装 PowerPoint 的内置适配器，不接管已打开的 PowerPoint。其他可靠宿主导出工具可用 `record-export` 登记实际 PDF 后检查。缺导出工具时保留 PPTX 与检查点，明确 PDF/视觉检查待完成。`finish` 自动运行构建、导出、渲染和机器检查；不会自动把未查看页面标为视觉通过。

## 交付

交付实际 PPTX、PDF 和完整讲者备注，附一个短检查记录。先核对科学映射和自动检查，再查看真实渲染；不能拿源码检查或旧缩略图冒充视觉检查。少于要求的页面、缺失关键图、字号溢出或未导出必须如实说明。最终回复只给文件、已完成范围与必要限制，不把内部检查流程全部推给用户。
