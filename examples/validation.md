# 验证记录

验证日期：2026-09-12。

环境：Windows、Python 3.12.14、本包独立 `.venv`、Microsoft YaHei、已安装的 Microsoft PowerPoint 导出器。安装时清除 PYTHONPATH/PYTHONHOME，禁用用户 site-packages；主要依赖使用 requirements.txt 的固定版本，没有从其他 skill 加载代码或 Python 包。

- 五页示例成功生成 PPTX、实际导出 PDF、渲染 1920×1080 PNG；逐页检查了文字、图像、分隔线、底部总结和字体，机器检查无错误、无警告。
- 三张图像从自制 PDF 截取，与源 PDF 重新渲染结果逐像素相同。
- 11 项行为测试通过：独立依赖位置；正文和裁图缓存复用；原图像素核验；修改图像并更新哈希后仍能识别篡改；只改一页只标记一页；锁定结论保护；缺失 panel 清单被拒绝；缺导出器时保留 PPTX 和待办；外部 PDF 不得绑定变化后的 PPTX；错误字体路径不被静默替换；安装更新备份与保留用户文件；拒绝覆盖不相关目录。其中缓存/像素核验在同一项测试中执行。
- 安装脚本支持空目录安装和现有版本备份后更新；不存在对其他技能路径的运行时依赖。

没有在本次验证中重新制作一整篇未见论文，也没有在 macOS/Linux 上实际运行；平台检测分支存在不等于全平台已经验证。当前机器未使用 LibreOffice，因此本次实际导出测试覆盖 Windows PowerPoint 路径。商业软件许可、宿主看图能力和图像生成权限仍由使用者环境提供。

示例全部为自制排版资料，不含真实论文或实验数据。示例验证支持“本包能够独立完成这些版式和导出检查”，不支持“任何模型、任何设备都能零条件生成同等科学质量”的说法。
